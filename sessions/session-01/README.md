# Session 01

Se instala en el visual studio code la extensión:

```
Name: p5.js 2.x Project Generator
Id: Irti.p5js-project-generator
Description: Generate new p5.js 2.x projects with online/offline library options. Includes autocomplete, IntelliSense, and parameter hints for most p5.js functions.
Version: 1.1.2
Publisher: IrtizaNasar
VS Marketplace Link: https://marketplace.visualstudio.com/items?itemName=Irti.p5js-project-generator
```

usando `Cmd` + `Shift` + `P` Podemos acceder al comando `>Create a p5.js project` que creará el boilerplate para crear un sketch.

Se explora la función setup y la función draw del framework de p5.js

Se hacen circulos (circle()), se experimentan otras figuras: square y triangle.

Se pide pintar las figuras de colores: Se presenta el fill

Se pide dibujar circulos en posiciones concretas (Y RELATIVAS) al tamaño del canvas.  Se descubre el height y el width.

Se experimenta con la función draw y se habla del bucle infinito.

Añadimos movimiento vertical a una de los circulos.

Añadimos colisiones a los bordes: primero con el centro y luego corregimos para que se haga con el perimetro.

Se deja como tarea añadir movimiento a los otros circulos

